apiVersion: v1
kind: Service
metadata:
  name: tarifas-especiales-service
  namespace: karting
spec:
  type: ClusterIP
  selector:
    app: tarifas-especiales-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
