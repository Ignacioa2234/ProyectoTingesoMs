apiVersion: v1
kind: Service
metadata:
  name: rack-service
  namespace: karting
spec:
  type: ClusterIP
  selector:
    app: rack-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
