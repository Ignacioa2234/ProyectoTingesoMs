apiVersion: apps/v1
kind: Deployment
metadata:
  name: descuentos-frecuentes-deploy
  namespace: karting
  labels:
    app: descuentos-frecuentes-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: descuentos-frecuentes-service
  template:
    metadata:
      labels:
        app: descuentos-frecuentes-service
    spec:
      containers:
        - name: descuentos-frecuentes-service
          image: descuentos-frecuentes-service:latest
          imagePullPolicy: Never
          env:
            - name: SERVER_PORT
              value: "8080"
          ports:
            - name: http
              containerPort: 8080
