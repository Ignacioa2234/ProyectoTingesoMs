apiVersion: v1
kind: Service
metadata:
  name: tarifas-service
  namespace: karting
spec:
  type: ClusterIP
  selector:
    app: tarifas-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
