server:
  port: 0

spring:
  application:
    name: tarifas-especiales-service

  datasource:
    url: jdbc:mysql://localhost:3306/tarifasespecialesdb
    username: root
    password: Ignacito1.
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1
    hostname: 127.0.0.1